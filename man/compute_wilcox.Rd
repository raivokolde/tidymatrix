% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute-across.R
\name{compute_wilcox}
\alias{compute_wilcox}
\title{Convenience wrapper for Wilcoxon rank-sum test}
\usage{
compute_wilcox(
  .data,
  group_col,
  control = NULL,
  treatment = NULL,
  log2 = TRUE,
  adjust = "fdr",
  add_to_data = FALSE,
  prefix = NULL,
  return_tibble = TRUE,
  ...
)
}
\arguments{
\item{.data}{A tidymatrix object with rows or columns active}

\item{group_col}{Character. Name of column in metadata containing group labels}

\item{control}{Character. Label for control group. If NULL, uses first unique value}

\item{treatment}{Character. Label for treatment group. If NULL, uses second unique value}

\item{log2}{Logical. If TRUE (default), computes log2 fold change. If FALSE,
computes raw fold change}

\item{adjust}{Character. Method for p-value adjustment. Default "fdr".
See \code{?p.adjust} for options. Use "none" for no adjustment}

\item{...}{Additional arguments passed to \code{wilcox.test()}}
}
\value{
A data.frame/tibble with columns: identifiers, p.value, log2fc (or fc),
median_diff, and p.adj (if adjustment applied)
}
\description{
Performs Wilcoxon rank-sum tests (Mann-Whitney U test) comparing two groups
for each row (or column) of a tidymatrix. This is a non-parametric alternative
to the t-test that doesn't assume normal distribution.
}
\examples{
mat <- matrix(rnorm(100), nrow = 10, ncol = 10)
col_data <- data.frame(
  sample = paste0("S", 1:10),
  condition = rep(c("Control", "Treatment"), each = 5)
)
row_data <- data.frame(gene = paste0("Gene", 1:10))
tm <- tidymatrix(mat, row_data, col_data)

# Wilcoxon test
results <- tm |>
  activate(rows) |>
  compute_wilcox(group_col = "condition")
}
