% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix-operations.R
\name{add_stats}
\alias{add_stats}
\title{Add statistics to metadata}
\usage{
add_stats(.data, ..., .fns = NULL, .names = NULL)
}
\arguments{
\item{.data}{A tidymatrix object with rows or columns active}

\item{...}{Functions to compute statistics (unquoted names like mean, var, sd)}

\item{.fns}{Alternative way to specify functions as a list}

\item{.names}{Names for the new columns. If NULL, uses function names.}
}
\value{
A tidymatrix object with statistics added to metadata
}
\description{
Compute statistics for the active dimension and add them as columns to
the corresponding metadata. Requires rows or columns to be active.
}
\examples{
mat <- matrix(rnorm(20), nrow = 4, ncol = 5)
tm <- tidymatrix(mat)

# Add row statistics
tm <- tm |>
  activate(rows) |>
  add_stats(mean, var, sd)

# Now row_data has columns: mean, var, sd

# Add column statistics
tm <- tm |>
  activate(columns) |>
  add_stats(median, min, max)

# Custom names
tm <- tm |>
  activate(rows) |>
  add_stats(mean, var, .names = c("row_mean", "row_var"))
}
