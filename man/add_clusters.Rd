% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{add_clusters}
\alias{add_clusters}
\title{Add cluster assignments to metadata (simple version)}
\usage{
add_clusters(x, method = c("hclust", "kmeans"), k, name = "cluster", ...)
}
\arguments{
\item{x}{A tidymatrix object}

\item{method}{Clustering method: "hclust" or "kmeans"}

\item{k}{Number of clusters}

\item{name}{Name prefix for cluster column. Default is "cluster".}

\item{...}{Additional arguments passed to the clustering function}
}
\value{
A tidymatrix object with cluster assignments added to metadata
}
\description{
A lightweight wrapper that adds cluster assignments without storing the
full clustering object.
}
\examples{
mat <- matrix(rnorm(100), nrow = 10, ncol = 10)
tm <- tidymatrix(mat)

# Quick clustering for exploration
tm <- tm |>
  activate(rows) |>
  add_clusters(method = "hclust", k = 3)
}
