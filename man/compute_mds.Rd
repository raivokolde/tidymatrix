% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dimreduction.R
\name{compute_mds}
\alias{compute_mds}
\title{Compute MDS on tidymatrix}
\usage{
compute_mds(
  x,
  name = NULL,
  k = 2,
  store = TRUE,
  dist_method = "euclidean",
  eig = FALSE,
  ...
)
}
\arguments{
\item{x}{A tidymatrix object}

\item{name}{Name for this analysis. Default is "row_mds" or "column_mds"
depending on active component.}

\item{k}{Number of dimensions for MDS embedding. Default is 2.}

\item{store}{If TRUE, stores the MDS result for later retrieval
with \code{get_analysis()}. Default is TRUE.}

\item{dist_method}{Distance method for dist(). Default is "euclidean".
Options: "euclidean", "maximum", "manhattan", "canberra", "binary", "minkowski".}

\item{eig}{If TRUE, return eigenvalues and GOF statistics (passed to cmdscale).}

\item{...}{Additional arguments passed to \code{stats::dist()} or
\code{stats::cmdscale()}}
}
\value{
A tidymatrix object with MDS coordinates added to metadata
}
\description{
Perform Classical Multidimensional Scaling on the matrix, adding MDS
coordinates to metadata and optionally storing the distance matrix and result.
}
\details{
This function wraps \code{stats::cmdscale()} and \code{stats::dist()}.
}
\examples{
mat <- matrix(rnorm(500), nrow = 50, ncol = 10)
row_data <- data.frame(id = 1:50)
tm <- tidymatrix(mat, row_data)

# MDS on rows
tm <- tm |>
  activate(rows) |>
  compute_mds(k = 2)

# Now row_data has row_mds_1, row_mds_2 columns

# Get MDS result
mds_obj <- get_analysis(tm, "row_mds")
}
