% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca.R
\name{add_pca_scores}
\alias{add_pca_scores}
\title{Add PCA scores to metadata (simple version)}
\usage{
add_pca_scores(x, n = 2, name = "PC", ...)
}
\arguments{
\item{x}{A tidymatrix object}

\item{n}{Number of components to compute. Default is 2.}

\item{name}{Name prefix for PC columns. Default is "PC".}

\item{...}{Additional arguments passed to \code{stats::prcomp()}}
}
\value{
A tidymatrix object with PC scores added to metadata
}
\description{
A lightweight version of \code{compute_prcomp} that only adds PC scores
to metadata without storing the full prcomp object.
}
\examples{
mat <- matrix(rnorm(100), nrow = 10, ncol = 10)
tm <- tidymatrix(mat)

# Quick PCA for visualization
tm <- tm |>
  activate(columns) |>
  add_pca_scores(n = 2, center = TRUE, scale. = TRUE)

# Now col_data has PC_PC1, PC_PC2
}
