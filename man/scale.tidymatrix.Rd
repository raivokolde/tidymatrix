% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix-operations.R
\name{scale.tidymatrix}
\alias{scale.tidymatrix}
\title{Scale rows or columns}
\usage{
\method{scale}{tidymatrix}(x, center = TRUE, scale = TRUE)
}
\arguments{
\item{x}{A tidymatrix object with rows or columns active}

\item{center}{If TRUE (default), center to mean = 0}

\item{scale}{If TRUE (default), scale to sd = 1}
}
\value{
A tidymatrix object with scaled matrix
}
\description{
Perform z-score scaling (center and scale to unit variance) on the active
dimension. Requires rows or columns to be active.
}
\examples{
mat <- matrix(rnorm(20, mean = 10, sd = 5), nrow = 4, ncol = 5)
tm <- tidymatrix(mat)

# Scale rows (z-score per row)
tm_scaled <- tm |>
  activate(rows) |>
  scale()

# Scale columns
tm_scaled <- tm |>
  activate(columns) |>
  scale()

# Only center, don't scale
tm_centered <- tm |>
  activate(rows) |>
  scale(center = TRUE, scale = FALSE)
}
