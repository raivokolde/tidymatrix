% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{compute_kmeans}
\alias{compute_kmeans}
\title{Compute k-means clustering on tidymatrix}
\usage{
compute_kmeans(x, centers, name = NULL, store = TRUE, ...)
}
\arguments{
\item{x}{A tidymatrix object}

\item{centers}{Number of clusters (k) or a set of initial cluster centers.}

\item{name}{Name for this analysis. Default is "row_kmeans" or "column_kmeans"
depending on active component.}

\item{store}{If TRUE, stores the full kmeans object for later retrieval
with \code{get_analysis()}. Default is TRUE.}

\item{...}{Additional arguments passed to \code{stats::kmeans()}, such as
\code{iter.max}, \code{nstart}, \code{algorithm}, etc.}
}
\value{
A tidymatrix object with cluster assignments added to metadata
}
\description{
Perform k-means clustering on the matrix, adding cluster assignments
to metadata and optionally storing the full kmeans object.
}
\details{
This function wraps \code{stats::kmeans()}, passing additional parameters
directly to it.
}
\examples{
mat <- matrix(rnorm(100), nrow = 10, ncol = 10)
row_data <- data.frame(id = 1:10)
tm <- tidymatrix(mat, row_data)

# K-means clustering with k=3
tm <- tm |>
  activate(rows) |>
  compute_kmeans(centers = 3, nstart = 25)

# Now row_data has row_kmeans_cluster column

# Get full kmeans object
km <- get_analysis(tm, "row_kmeans")
km$tot.withinss  # Total within-cluster sum of squares
}
