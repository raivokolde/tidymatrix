% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix-operations.R
\name{transform.tidymatrix}
\alias{transform.tidymatrix}
\title{Transform matrix values}
\usage{
\method{transform}{tidymatrix}(.data, ...)
}
\arguments{
\item{.data}{A tidymatrix object with matrix active}

\item{...}{Transformation expression using \code{.} to refer to the matrix}
}
\value{
A tidymatrix object with transformed matrix
}
\description{
Apply an element-wise transformation to the entire matrix. Requires
matrix to be active.
}
\examples{
mat <- matrix(1:12, nrow = 3, ncol = 4)
tm <- tidymatrix(mat)

# Log transform
tm_log <- tm |>
  activate(matrix) |>
  transform(log2(. + 1))

# Square root
tm_sqrt <- tm |>
  activate(matrix) |>
  transform(sqrt(.))

# Custom transformation
tm_custom <- tm |>
  activate(matrix) |>
  transform(pmin(., 10))  # Cap at 10
}
