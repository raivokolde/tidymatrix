% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix-operations.R
\name{t.tidymatrix}
\alias{t.tidymatrix}
\title{Transpose a tidymatrix}
\usage{
\method{t}{tidymatrix}(x)
}
\arguments{
\item{x}{A tidymatrix object}
}
\value{
A tidymatrix object with transposed matrix and swapped metadata
}
\description{
Transpose the matrix and swap row and column metadata. This operation
works regardless of which component is active. Uses base R's \code{t()}
generic, so it works correctly even when tidyverse is loaded.
}
\examples{
mat <- matrix(1:12, nrow = 3, ncol = 4)
row_data <- data.frame(gene = paste0("G", 1:3))
col_data <- data.frame(sample = paste0("S", 1:4))
tm <- tidymatrix(mat, row_data, col_data)

# Transpose: genes x samples -> samples x genes
tm_t <- t(tm)
dim(tm_t$matrix)  # Now 4 x 3
}
