% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count.R
\name{tally.grouped_tidymatrix}
\alias{tally.grouped_tidymatrix}
\title{Count observations in groups}
\usage{
\method{tally}{grouped_tidymatrix}(
  x,
  wt = NULL,
  sort = FALSE,
  name = NULL,
  .matrix_fn = NULL,
  .matrix_args = list()
)
}
\arguments{
\item{x}{A grouped_tidymatrix object}

\item{wt}{Frequency weights (not yet implemented)}

\item{sort}{If TRUE, sort output in descending order of n}

\item{name}{Name of count column (default: "n")}

\item{.matrix_fn}{Function to aggregate matrix values. Default is \code{mean}
for numeric matrices. Required for non-numeric matrices.}

\item{.matrix_args}{List of additional arguments to pass to \code{.matrix_fn}}
}
\value{
A tidymatrix object with counts and aggregated matrix
}
\description{
Count observations within existing groups. This is a wrapper around
\code{summarize(n = n())}.
}
\examples{
\dontrun{
mat <- matrix(rnorm(20), nrow = 10, ncol = 2)
row_data <- data.frame(
  id = 1:10,
  group = rep(c("A", "B"), each = 5)
)
tm <- tidymatrix(mat, row_data)

# Tally within groups
tm |>
  activate(rows) |>
  group_by(group) |>
  tally()
}
}
