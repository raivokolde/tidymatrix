% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute-across.R
\name{compute_anova}
\alias{compute_anova}
\title{Convenience wrapper for ANOVA}
\usage{
compute_anova(
  .data,
  group_col,
  adjust = "fdr",
  add_to_data = FALSE,
  prefix = NULL,
  return_tibble = TRUE
)
}
\arguments{
\item{.data}{A tidymatrix object with rows or columns active}

\item{group_col}{Character. Name of column in metadata containing group labels}

\item{adjust}{Character. Method for p-value adjustment. Default "fdr".
Use "none" for no adjustment}
}
\value{
A data.frame/tibble with columns: identifiers, f.statistic, p.value,
df_between, df_within, and p.adj (if adjustment applied)
}
\description{
Performs one-way ANOVA for each row (or column) of a tidymatrix to test
for differences across multiple groups.
}
\examples{
mat <- matrix(rnorm(150), nrow = 10, ncol = 15)
col_data <- data.frame(
  sample = paste0("S", 1:15),
  condition = rep(c("A", "B", "C"), each = 5)
)
row_data <- data.frame(gene = paste0("Gene", 1:10))
tm <- tidymatrix(mat, row_data, col_data)

# ANOVA
results <- tm |>
  activate(rows) |>
  compute_anova(group_col = "condition")
}
