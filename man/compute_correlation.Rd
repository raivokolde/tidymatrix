% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute-across.R
\name{compute_correlation}
\alias{compute_correlation}
\title{Convenience wrapper for correlation tests}
\usage{
compute_correlation(
  .data,
  var,
  method = "pearson",
  adjust = "fdr",
  add_to_data = FALSE,
  prefix = NULL,
  return_tibble = TRUE
)
}
\arguments{
\item{.data}{A tidymatrix object with rows or columns active}

\item{var}{Character. Name of continuous variable in metadata to correlate with}

\item{method}{Character. Correlation method: "pearson" (default), "spearman",
or "kendall"}

\item{adjust}{Character. Method for p-value adjustment. Default "fdr".
Use "none" for no adjustment}
}
\value{
A data.frame/tibble with columns: identifiers, correlation, p.value,
and p.adj (if adjustment applied)
}
\description{
Performs correlation tests between each row (or column) and a continuous
variable in the metadata.
}
\examples{
mat <- matrix(rnorm(100), nrow = 10, ncol = 10)
col_data <- data.frame(
  sample = paste0("S", 1:10),
  age = rnorm(10, 50, 10)
)
row_data <- data.frame(gene = paste0("Gene", 1:10))
tm <- tidymatrix(mat, row_data, col_data)

# Pearson correlation
results <- tm |>
  activate(rows) |>
  compute_correlation(var = "age", method = "pearson")

# Spearman correlation
results <- tm |>
  activate(rows) |>
  compute_correlation(var = "age", method = "spearman")
}
